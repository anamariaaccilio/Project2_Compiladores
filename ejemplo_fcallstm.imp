fun int suma(int x)
  var int accum;
  accum = 0;
  print(15);
  while 0 < x do
    var int zz;
    accum = accum + x;
    x = x - 1
  endwhile;
  print(16);
  return (accum)
endfun

fun void imprime(int x, int y)
    var int z;
    z = x + y;
    print(z);
    return()
endfun

fun void main()
    var int x; 
    x = 10; 
    suma(x);
    imprime(x, x + 10);
    return()
endfun